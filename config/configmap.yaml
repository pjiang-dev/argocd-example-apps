apiVersion: v1
kind: ConfigMap
metadata:
  name: test-managed-fields
  namespace: default
  annotations:
    argocd.argoproj.io/tracking-id: "test-app:ConfigMap:default/test-managed-fields"
    argocd.argoproj.io/sync-wave: "-1"  # ← New annotation
    test.example.com/new-annotation: "test-value"  # ← Another new one
data:
  initial-key: "initial-value"
